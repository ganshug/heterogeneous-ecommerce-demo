---
# =============================================================================
# IBM Entitlement Key Secret — for pulling IBM Db2 images from cp.icr.io
#
# IMPORTANT: This file is a TEMPLATE. Do NOT commit actual credentials to git.
#
# The deploy.sh script will create this secret automatically when you provide
# your IBM Entitlement Key via IBM_ENTITLEMENT_KEY environment variable or
# interactive prompt.
#
# To create manually:
#   oc create secret docker-registry ibm-entitlement-key \
#     --docker-server=cp.icr.io \
#     --docker-username=cp \
#     --docker-password=<YOUR_IBM_ENTITLEMENT_KEY> \
#     --namespace=ecommerce-demo
#
# Obtain your IBM Entitlement Key from:
#   https://myibm.ibm.com/products-services/containerlibrary
#
# NOTE: If the IBM Entitlement Key is already in the OCP global pull secret
# (openshift-config/pull-secret), the Db2 Operator will use it automatically.
# This namespace-level secret is an additional fallback.
# =============================================================================
# This file is intentionally left without credentials.
# The deploy.sh script creates the secret dynamically.
